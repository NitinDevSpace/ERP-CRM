const useAppSettings = () => {
  let settings = {};
  settings['idurar_app_email'] = 'SSample@domain.com';
  settings['idurar_base_url'] = 'https://cloud.com';
  return settings;
};

module.exports = useAppSettings;
